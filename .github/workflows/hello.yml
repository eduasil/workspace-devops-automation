name: Hello GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Exbir mensagem no log
        run: echo "✅ GitHub Actions funcionando com sucesso!"
